# Implementation plan

## Integration contract

**Entrypoint:** `linkshelf/cmd/server/main.go`

**Startup / dependency order (SPEC + architecture):**

- Open SQLite file `linkshelf.db` in the current working directory.
- `InitSchema(db)` then `store.DB = db`.
- Register handlers and static routes.
- `http.ListenAndServe(":8080", nil)` and log `listening on :8080`.
- `cd linkshelf && go test ./...` — green (packages without `*_test.go` still compile).
- `cd linkshelf && go run ./cmd/server` — UI loads; add one link, see it, delete it.
- `npx playwright test` — UI e2e tests pass (verify UI loads, add/delete links, static file serving).
- **Data model** (`linkshelf/internal/store/schema.go`) — defines the `Link` struct

**HTTP routes (SPEC verbatim):**

| Method | Path |
|--------|------|
| GET | `/` |
| GET | `/static/{file}` |
| GET | `/api/links` |
| POST | `/api/links` |
| DELETE | `/api/links/{id}` |

**Per-file exports (from architecture.md):**

- linkshelf/internal/store/schema.go
- linkshelf/internal/store/store.go
- linkshelf/internal/api/handlers.go
- linkshelf/cmd/server/main.go

## Bead map

_Generated by gastown planning sync (open bead IDs from `bd list`). Expand acceptance bullets if QA requires more detail._

### te-10q: linkshelf/cmd/server/main.go
- Scope: Implement `linkshelf/cmd/server/main.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Verify: `cd linkshelf && go mod tidy && go build ./...`
- Acceptance:
  - File exists at `linkshelf/cmd/server/main.go` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-10q` is closed only after verification passes.

## Phase context

Active delivery phase `smoke-test` only. Later phases start automatically after QA `all_passed` (or use `gt rig set-phase` to switch manually).

