# Implementation plan

## Bead map

_Generated by gastown planning sync (open bead IDs from `bd list`). Expand acceptance bullets if QA requires more detail._

### te-6ko: linkshelf/internal/api/handlers.go
- Scope: Implement `linkshelf/internal/api/handlers.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Verify: `cd linkshelf && go mod tidy && go test -timeout 30s -count=1 ./internal/api/...`
- Acceptance:
  - File exists at `linkshelf/internal/api/handlers.go` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Package has unit tests in `linkshelf/internal/api/handlers_test.go` (same bead or dedicated test bead) covering SPEC behavior before close.
  - `go test` for this package must pass (Verify runs `go test -count=1 ./<pkg>/...`).
  - Bead `te-6ko` is closed only after verification passes.

### te-0po: linkshelf/internal/api/handlers_test.go
- Scope: Implement `linkshelf/internal/api/handlers_test.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Verify: `cd linkshelf && go mod tidy && go test -timeout 30s -count=1 ./internal/api/...`
- Acceptance:
  - Unit tests assert **functional requirements** from SPEC.md and this plan section (happy path, errors, edge cases named in architecture).
  - No trivial always-pass tests (`assert True`, empty `TestX` bodies, or tests that only import the module).
  - Test names and cases map to SPEC/plan acceptance bullets (reference FR IDs or behavior in comments when helpful).
  - File exists at `linkshelf/internal/api/handlers_test.go` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-0po` is closed only after verification passes.

## Phase context

Active delivery phase `api-handlers` only. Later phases start automatically after QA `all_passed` (or use `gt rig set-phase` to switch manually).

