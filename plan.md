# Implementation plan

## Bead map

_Generated by gastown planning sync (open bead IDs from `bd list`). Expand acceptance bullets if QA requires more detail._

### te-4s8: linkshelf/go.mod
- Scope: Implement `linkshelf/go.mod` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Verify: `cd linkshelf && go mod download`
- Acceptance:
  - File exists at `linkshelf/go.mod` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-4s8` is closed only after verification passes.

### te-7y3: linkshelf/internal/store/schema.go
- Scope: Implement `linkshelf/internal/store/schema.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Verify: `cd linkshelf && go mod tidy && go test -timeout 30s -count=1 ./internal/store/...`
- Acceptance:
  - File exists at `linkshelf/internal/store/schema.go` under the rig worktree.
  - Exports `InitSchema(*sql.DB) error` (or equivalent) with `CREATE TABLE IF NOT EXISTS` DDL from architecture.md.
  - DDL matches architecture.md; production entrypoint and package tests call this helper — no duplicated `CREATE TABLE` strings in tests or main.
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead is implemented before `linkshelf/internal/store/store.go` in build order.
  - Bead `te-7y3` is closed only after verification passes.

### te-d9u: linkshelf/internal/store/schema_test.go
- Scope: Implement `linkshelf/internal/store/schema_test.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Verify: `cd linkshelf && go mod tidy && go test -timeout 30s -count=1 ./internal/store/...`
- Acceptance:
  - Unit tests assert **functional requirements** from SPEC.md and this plan section (happy path, errors, edge cases named in architecture).
  - No trivial always-pass tests (`assert True`, empty `TestX` bodies, or tests that only import the module).
  - Test names and cases map to SPEC/plan acceptance bullets (reference FR IDs or behavior in comments when helpful).
  - File exists at `linkshelf/internal/store/schema_test.go` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-d9u` is closed only after verification passes.

## Phase context

Active delivery phase `backend-setup` only. Later phases start automatically after QA `all_passed` (or use `gt rig set-phase` to switch manually).

