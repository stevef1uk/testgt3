# Implementation plan

## Bead map

_Generated by gastown planning sync (open bead IDs from `bd list`). Expand acceptance bullets if QA requires more detail._

### te-37c: linkshelf/package.json
- Scope: Implement `linkshelf/package.json` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Verify: `cd linkshelf && go mod tidy && go build ./...`
- Acceptance:
  - File exists at `linkshelf/package.json` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-37c` is closed only after verification passes.

### te-k8r: linkshelf/playwright.config.js
- Scope: Implement `linkshelf/playwright.config.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `linkshelf/playwright.config.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-k8r` is closed only after verification passes.

### te-5pm: linkshelf/tests/e2e.spec.js
- Scope: Implement `linkshelf/tests/e2e.spec.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `linkshelf/tests/e2e.spec.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-5pm` is closed only after verification passes.

## Phase context

Active delivery phase `e2e-testing` only. Later phases start automatically after QA `all_passed` (or use `gt rig set-phase` to switch manually).

