# Implementation plan

## Bead map

_Generated by gastown planning sync (open bead IDs from `bd list`). Expand acceptance bullets if QA requires more detail._

### te-s92: linkshelf/internal/store/store.go
- Scope: Implement `linkshelf/internal/store/store.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Verify: `cd linkshelf && go mod tidy && go test -timeout 30s -count=1 ./internal/store/...`
- Acceptance:
  - File exists at `linkshelf/internal/store/store.go` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Store methods match SPEC (`List`/`Create`/`Delete` with `context.Context`); tests use `:memory:` + `InitSchema` — not a shared `./links.db`.
  - Bead `te-s92` is closed only after verification passes.

## Phase context

Active delivery phase `store-phase` only. Later phases start automatically after QA `all_passed` (or use `gt rig set-phase` to switch manually).

