# Implementation plan

## Integration contract

**Entrypoint:** `linkshelf/cmd/server/main.go`

**Startup / dependency order (SPEC + architecture):**

- Open SQLite file `linkshelf.db` in the current working directory.
- `InitSchema(db)` then `store.DB = db`.
- Register handlers and static routes.
- `http.ListenAndServe(":8080", nil)` and log `listening on :8080`.
- `cd linkshelf && go test ./...` — green (packages without `*_test.go` still compile).
- `cd linkshelf && go run ./cmd/server` — UI loads; add one link, see it, delete it.
- `npx playwright test` — UI e2e tests pass (verify UI loads, add/delete links, static file serving).
- **Data model** (`linkshelf/internal/store/schema.go`) — defines the `Link` struct

**HTTP routes (SPEC verbatim):**

| Method | Path |
|--------|------|
| GET | `/` |
| GET | `/static/{file}` |
| GET | `/api/links` |
| POST | `/api/links` |
| DELETE | `/api/links/{id}` |

**Per-file exports (from architecture.md):**

- linkshelf/internal/store/schema.go
- linkshelf/internal/store/store.go
- linkshelf/internal/api/handlers.go
- linkshelf/cmd/server/main.go

## Bead map

_Generated by gastown planning sync (open bead IDs from `bd list`). Expand acceptance bullets if QA requires more detail._

### te-w8m: linkshelf/internal/api/handlers.go
- Scope: Implement `linkshelf/internal/api/handlers.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Verify: `cd linkshelf && go mod tidy && go test -timeout 30s -count=1 ./internal/api/... && go build ./cmd/server/...`
- Acceptance:
  - File exists at `linkshelf/internal/api/handlers.go` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Package has unit tests in `linkshelf/internal/api/handlers_test.go` (same bead or dedicated test bead) covering SPEC behavior before close.
  - `go test` for this package must pass (Verify runs `go test -count=1 ./<pkg>/...`).
  - Bead `te-w8m` is closed only after verification passes.

### te-ezs: linkshelf/internal/api/handlers_test.go
- Scope: Implement `linkshelf/internal/api/handlers_test.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Verify: `cd linkshelf && go mod tidy && go test -timeout 30s -count=1 ./internal/api/...`
- Acceptance:
  - Unit tests assert **functional requirements** from SPEC.md and this plan section (happy path, errors, edge cases named in architecture).
  - No trivial always-pass tests (`assert True`, empty `TestX` bodies, or tests that only import the module).
  - Test names and cases map to SPEC/plan acceptance bullets (reference FR IDs or behavior in comments when helpful).
  - File exists at `linkshelf/internal/api/handlers_test.go` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-ezs` is closed only after verification passes.

### te-l6p: linkshelf/cmd/server/main.go
- Scope: Implement `linkshelf/cmd/server/main.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Verify: `cd linkshelf && go mod tidy && go build ./...`
- Acceptance:
  - File exists at `linkshelf/cmd/server/main.go` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-l6p` is closed only after verification passes.

## Phase context

Active delivery phase `backend-api` only. Later phases start automatically after QA `all_passed` (or use `gt rig set-phase` to switch manually).

